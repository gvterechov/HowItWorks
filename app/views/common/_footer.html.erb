<div class="ui inverted vertical footer segment"
     style="bottom: 0; width: 100%; position: absolute;"
>
  <%= t('copyright') %>

  <% if current_user&.admin? %>
    <div class="ui small header" style="margin-top: 0.5em;">
      <%= link_to t('edit_users'), users_path(locale: I18n.locale), class: 'ui inverted link' %>
    </div>
  <% end %>
</div>

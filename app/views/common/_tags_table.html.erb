<div class="ui aligned basic segment">
  <div class="ui centered grid">
    <h3>
      <%= t('my_tags') %>
      <%= link_to "<i class='plus icon'></i>#{t('new_tag', default: 'Новый тег')}".html_safe,
                  new_task_tag_path,
                  class: 'ui basic button' %>
    </h3>

    <div class="row">
      <table class="ui selectable striped celled table">
        <thead>
          <tr>
            <th>#</th>
            <th><%= t('tags_name') %></th>
            <th class="center aligned"><%= t('actions') %></th>
          </tr>
        </thead>
        <tbody>
          <% @tags.each_with_index do |tag, index| %>
            <tr>
              <td><%= index + 1 %></td>
              <td><%= tag.name %></td>
              <td class="center aligned">
                <div class="ui buttons">
                    <%= link_to expressions_edit_tags_path(id: tag.id), class: 'ui tiny basic primary button' do %>
                        <i class="edit icon"></i> <%= t('edit') %>
                    <% end %>

                    <%= button_to expressions_destroy_tag_path(id: tag.id),
                        method: :delete,
                        data: { confirm: 'Вы уверены, что хотите удалить тег?' },
                        class: 'ui red basic button' do %>
                    <i class="trash icon"></i>
                        <%= link_to t('delete') =%>
                    <% end %>
                </div>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>